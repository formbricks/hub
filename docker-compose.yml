services:
  # PostgreSQL database with pgvector extension for Hub
  postgres:
    image: pgvector/pgvector:pg18
    container_name: formbricks_postgres
    environment:
      POSTGRES_USER: formbricks
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-formbricks_dev}
      POSTGRES_DB: hub_dev
    ports:
      - '5432:5432'
    volumes:
      - postgres_data:/var/lib/postgresql
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U formbricks -d hub_dev']
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - formbricks
    command: >
      postgres
      -c shared_preload_libraries=vector

volumes:
  postgres_data:
    driver: local

networks:
  formbricks:
    driver: bridge

