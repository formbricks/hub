# The SDK is a build artifact: the generator config is committed, its output is not.
# See AGENTS.md -> TypeScript SDK. Regenerate with `pnpm generate`.
src/generated/
dist/

node_modules/
*.tgz

# Scratch from scripts/prepare-comparison.mjs: the packed tarballs and both
# extracted packages the release and preview workflows compare.
.compare/
